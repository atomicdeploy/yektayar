/**
 * Shared constants for YektaYar platform
 */

/**
 * Socket.IO path configuration
 * Used consistently across backend, admin-panel, and mobile-app
 */
export const SOCKET_IO_PATH = '/socket.io/'
