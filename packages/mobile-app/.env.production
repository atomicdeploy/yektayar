# Production environment configuration
API_BASE_URL=https://api.yektayar.ir
VITE_ENVIRONMENT=production

# Proxy domain for HMR WebSocket when behind reverse proxy
# This ensures Vite's HMR connects through the proxy domain instead of localhost
VITE_PROXY_DOMAIN=app.yektayar.ir
