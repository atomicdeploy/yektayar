# Development environment configuration
API_BASE_URL=http://localhost:3000
VITE_ENVIRONMENT=development

# Skip API validation (for UI testing only - not recommended for normal development)
# Uncomment the line below ONLY when you need to test UI without a running backend
# VITE_SKIP_API_VALIDATION=true

# HMR WebSocket Port (for reverse proxy deployments)
# Leave unset for local development (uses dev server port)
# Set to 443 for HTTPS reverse proxy, 80 for HTTP reverse proxy
# VITE_HMR_CLIENT_PORT=443
