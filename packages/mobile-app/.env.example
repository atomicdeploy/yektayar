# Development environment configuration
API_BASE_URL=http://localhost:3000
VITE_ENVIRONMENT=development

# Skip API validation (for UI testing only - not recommended for normal development)
# Uncomment the line below ONLY when you need to test UI without a running backend
# VITE_SKIP_API_VALIDATION=true

# Mobile App Base URL (for reverse proxy deployments)
# Set this when deploying behind a reverse proxy. HMR port is automatically detected.
# Leave empty for local development.
# Examples:
#   MOBILE_APP_BASE_URL=https://app.yektayar.ir (auto-detects port 443)
#   MOBILE_APP_BASE_URL=http://app.yektayar.ir (auto-detects port 80)
#   MOBILE_APP_BASE_URL=https://app.yektayar.ir:8443 (uses custom port 8443)
# MOBILE_APP_BASE_URL=
