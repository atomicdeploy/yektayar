# Development environment configuration
API_BASE_URL=http://localhost:3000
VITE_ENVIRONMENT=development

# Optional: Set this when running behind a reverse proxy (e.g., nginx, apache, caddy)
# This configures Vite's HMR WebSocket to use the proxy domain instead of localhost
# Example: VITE_PROXY_DOMAIN=app.yektayar.ir
# Leave empty to auto-detect from browser Host header (recommended)
# VITE_PROXY_DOMAIN=
