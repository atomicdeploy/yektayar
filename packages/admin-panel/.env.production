# Production environment configuration
API_BASE_URL=https://api.yektayar.ir
VITE_ENVIRONMENT=production

# Proxy domain for HMR WebSocket when behind reverse proxy
# Leave empty to auto-detect from browser Host header (recommended)
# Only set this if you need to override auto-detection
# VITE_PROXY_DOMAIN=
