# Development environment configuration
API_BASE_URL=http://localhost:3000
VITE_ENVIRONMENT=development

# Admin Panel Base URL (for reverse proxy deployments)
# Set this when deploying behind a reverse proxy. HMR port is automatically detected.
# Leave empty for local development.
# Examples:
#   ADMIN_PANEL_BASE_URL=https://panel.yektayar.ir (auto-detects port 443)
#   ADMIN_PANEL_BASE_URL=http://panel.yektayar.ir (auto-detects port 80)
#   ADMIN_PANEL_BASE_URL=https://panel.yektayar.ir:8443 (uses custom port 8443)
# ADMIN_PANEL_BASE_URL=
