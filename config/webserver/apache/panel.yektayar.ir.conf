<VirtualHost *:80>
    ServerName panel.yektayar.ir
    
    # Redirect to HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}$1 [R=301,L]
</VirtualHost>

<VirtualHost *:443>
    ServerName panel.yektayar.ir
    
    # SSL Configuration (will be configured by Certbot)
    # SSLEngine on
    # SSLCertificateFile /etc/letsencrypt/live/panel.yektayar.ir/fullchain.pem
    # SSLCertificateKeyFile /etc/letsencrypt/live/panel.yektayar.ir/privkey.pem
    # Include /etc/letsencrypt/options-ssl-apache.conf
    
    # Security Headers
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Enable proxy modules
    ProxyPreserveHost On
    ProxyTimeout 300
    
    # Proxy to Admin Panel dev server
    ProxyPass / http://127.0.0.1:5173/
    ProxyPassReverse / http://127.0.0.1:5173/
    
    # WebSocket Support (for Vite HMR)
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule ^/(.*)$ ws://127.0.0.1:5173/$1 [P,L]
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule ^/(.*)$ http://127.0.0.1:5173/$1 [P,L]
    
    # Custom Error Pages
    ErrorDocument 403 /error-pages/403.html
    ErrorDocument 404 /error-pages/404.html
    ErrorDocument 500 /error-pages/500.html
    ErrorDocument 502 /error-pages/502.html
    ErrorDocument 503 /error-pages/503.html
    ErrorDocument 504 /error-pages/504.html
    
    # Error pages directory (served directly, not proxied)
    Alias /error-pages /var/www/yektayar/error-pages
    <Directory /var/www/yektayar/error-pages>
        Options -Indexes
        AllowOverride None
        Require all granted
    </Directory>
    
    # Logging
    ErrorLog ${APACHE_LOG_DIR}/panel.yektayar.ir_error.log
    CustomLog ${APACHE_LOG_DIR}/panel.yektayar.ir_access.log combined
</VirtualHost>
