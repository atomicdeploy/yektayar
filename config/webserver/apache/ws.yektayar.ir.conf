<VirtualHost *:80>
    ServerName ws.yektayar.ir
    
    # Redirect to HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}$1 [R=301,L]
</VirtualHost>

<VirtualHost *:443>
    ServerName ws.yektayar.ir
    
    # SSL Configuration (will be configured by Certbot)
    # SSLEngine on
    # SSLCertificateFile /etc/letsencrypt/live/ws.yektayar.ir/fullchain.pem
    # SSLCertificateKeyFile /etc/letsencrypt/live/ws.yektayar.ir/privkey.pem
    # Include /etc/letsencrypt/options-ssl-apache.conf
    
    # Security Headers
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # CORS Headers for WebSocket
    Header always set Access-Control-Allow-Origin "*"
    Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    Header always set Access-Control-Max-Age "86400"
    
    # Handle OPTIONS requests for CORS preflight
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=204,L]
    
    # Enable proxy modules
    ProxyPreserveHost On
    ProxyTimeout 300
    
    # WebSocket Proxy - forward all requests to dedicated WebSocket server
    ProxyPass / http://127.0.0.1:3500/
    ProxyPassReverse / http://127.0.0.1:3500/
    
    # WebSocket Support
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule ^/(.*)$ ws://127.0.0.1:3500/$1 [P,L]
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule ^/(.*)$ http://127.0.0.1:3500/$1 [P,L]
    
    # Logging
    ErrorLog ${APACHE_LOG_DIR}/ws.yektayar.ir_error.log
    CustomLog ${APACHE_LOG_DIR}/ws.yektayar.ir_access.log combined
    
    # Log WebSocket upgrade attempts
    LogLevel warn rewrite:trace3
</VirtualHost>
